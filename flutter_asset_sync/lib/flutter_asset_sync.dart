/// Sync Figma/exported assets directly into Flutter projects.
class FlutterAssetSync {
  FlutterAssetSync._();

  static const String packageName = 'flutter_asset_sync';

  /// Scaffold entry — replace with real implementation.
  static void init() {}
}
