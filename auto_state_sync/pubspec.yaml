name: auto_state_sync
description: Automatic API → local state → cache synchronization.
version: 0.1.0

topics:
  - auto-state-sync
  - cache
  - dart
  - flutter
  - mobile
  - offline
  - pub
  - state
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

repository: https://github.com/NPM-Packages-Modules/auto_state_sync
