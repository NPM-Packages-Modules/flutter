/// Convert screenshots into Flutter UI code.
class FlutterUiCloner {
  FlutterUiCloner._();

  static const String packageName = 'flutter_ui_cloner';

  /// Scaffold entry — replace with real implementation.
  static void init() {}
}
