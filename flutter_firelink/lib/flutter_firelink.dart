/// Firebase setup in one command.
class FlutterFirelink {
  FlutterFirelink._();

  static const String packageName = 'flutter_firelink';

  /// Scaffold entry — replace with real implementation.
  static void init() {}
}
